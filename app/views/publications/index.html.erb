
<% content_for :title, "Posts" %>

<%= react_component("PostsThread", {course_id: Course.first.id, current_user: current_user,
          current_user_avatar: current_user.avatar.attached? ? rails_representation_url(current_user.avatar.variant(resize: "100x100"), only_path: true)
                                   : image_url("dragon.png")
}) %>

